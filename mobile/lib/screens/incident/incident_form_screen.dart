import 'package:flutter/material.dart';

class IncidentFormScreen extends StatelessWidget {
  const IncidentFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Incident')),
      body: const Center(
        child: Text('Incident form belum tersedia'),
      ),
    );
  }
}
