import 'package:flutter/material.dart';

class FireAlarmScreen extends StatelessWidget {
  const FireAlarmScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fire Alarm Inspection')),
      body: const Center(
        child: Text('Fire Alarm Inspection belum tersedia'),
      ),
    );
  }
}
