# SHE Inspection System - Claude Memory
> Last analyzed: 2026-08-06
> Primary project context: `PROJECT_CONTEXT.md`

## What this project is
Project analyzed by codebase-memory

## Quick reference
- **Mobile**: Flutter/Dart in `mobile/`
- **Backend**: Laravel 11/PHP in `backend/she-api/`
- **Auth**: Laravel Sanctum Bearer token
- **Database**: MySQL, managed by Laravel migrations
- **Tests**: `flutter test` and `php artisan test`

## Context document

Read `PROJECT_CONTEXT.md` before analyzing or changing the project. It contains
the folder map, entry points, data flow, stack, modules, API routes, data model
notes, commands, conventions, and known mismatches.

## Instruction
Use the context document for project structure, then read only the relevant
source files for the current task. Treat actual routes/controllers/models as the
source of truth when documentation conflicts with implementation.
