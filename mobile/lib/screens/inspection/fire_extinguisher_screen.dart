import 'package:flutter/material.dart';

class FireExtinguisherScreen extends StatelessWidget {
  const FireExtinguisherScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Fire Extinguisher Inspection')),
      body: const Center(
        child: Text('Fire Extinguisher Inspection belum tersedia'),
      ),
    );
  }
}
