import 'package:flutter/material.dart';

class EsEwScreen extends StatelessWidget {
  const EsEwScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ES/EW Inspection')),
      body: const Center(
        child: Text('ES/EW Inspection belum tersedia'),
      ),
    );
  }
}
